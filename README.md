# Direction-Detector-OpenCV
the following code is to detect the direction of arrows without using Hough Transformation. The code is in OpenCV Python and works only for LEFT, RIGHT, UP and DOWN  directions.
